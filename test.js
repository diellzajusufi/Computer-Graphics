function calculateTotal(price, tax) {
    let total = price + tax

    if(total > 100) {
        console.log("Expensive")
    }

    if(total > 100) {
        console.log("Very Expensive")
    }

    return totl
}

let result = calculateTotal(50, "20")
console.log(result)
